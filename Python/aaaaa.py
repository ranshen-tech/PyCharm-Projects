import json
# 准备列表，列表中每一个元素都是字典，将其转换为json
data = [{'name': 'rs', 'age': 19}, {'name': 'cyy', 'age': 19}]
# data = [{'name':'rs', age = 29}, {'name':'zyc', age = 38}, {'name':'cyy', age = 16}]
# 通过json.dump(data)方法把python数据转化为json数据
# data = json.dump(data)
# 通过json.load(data)方法
